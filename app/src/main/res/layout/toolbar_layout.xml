<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/toolbar"
    app:titleTextColor="@color/toolLeftText"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/colorPrimary"
    >
    <!--自定义toolbar的title 和subtitle -->


    <TextView
        android:id="@+id/toolbar_tv_title"
        style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:lines="1"
        android:ellipsize="end"
        android:text="title"
        android:scrollHorizontally="true"
        android:textColor="@color/toolCenterText"
        android:layout_gravity="center" />

    <TextView
        android:id="@+id/toolbar_tv_left"
        style="@style/TextAppearance.AppCompat.Widget.ActionBar.Subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:singleLine="true"
        android:textColor="@color/toolLeftText"
        android:text="subtitle"
        android:paddingRight="10dp"
        android:layout_gravity="left" />

    <TextView
        android:id="@+id/toolbar_tv_right"
        style="@style/TextAppearance.AppCompat.Widget.ActionBar.Subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:lines="1"
        android:ellipsize="end"
        android:text="sub2title"
        android:scrollHorizontally="true"
        android:layout_gravity="right"
        android:textColor="@color/toolRightText" />
</android.support.v7.widget.Toolbar>